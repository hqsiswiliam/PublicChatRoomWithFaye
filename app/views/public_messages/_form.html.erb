<%= form_for(@public_message) do |f| %>
  <% if @public_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@public_message.errors.count, "error") %> prohibited this public_message from being saved:</h2>

      <ul>
      <% @public_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :public_message %><br>
    <%= f.text_area :public_message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
